#include <basic.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>


basic_return basic_run(char *fn, char * buff)
{
  printf("%s\n", buff);
  struct basic_return return_val;
  return return_val;
}
